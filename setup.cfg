[metadata]
name = DockQ

[options]
package_dir =
    =src
packages = find:
install_requires =
    numpy
    biopython>=1.78

[options.packages.find]
where=src

[options.entry_points]
console_scripts =
    DockQ = DockQ.__main__:main

